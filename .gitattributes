# Ensure shell scripts have Unix line endings
bin/wr-* text eol=lf
install.sh text eol=lf

# Windows batch files keep CRLF
*.cmd text eol=crlf

# PowerShell scripts are cross-platform, keep as-is
*.ps1 text
