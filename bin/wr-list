#!/usr/bin/env bash
# Wrapper for list-secrets.ps1
REAL_SCRIPT="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd "$(dirname "$REAL_SCRIPT")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
exec pwsh -NoProfile -ExecutionPolicy Bypass -File "$PROJECT_ROOT/scripts/list-secrets.ps1" "$@"
