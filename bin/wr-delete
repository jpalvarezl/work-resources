#!/usr/bin/env bash
# Wrapper for delete-secret.ps1
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
exec pwsh -NoProfile -ExecutionPolicy Bypass -File "$PROJECT_ROOT/scripts/delete-secret.ps1" "$@"
